#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char strg[] = "\"Programming is like building a multilingual puzzle";

	puts(strg);

	return (0);
}
