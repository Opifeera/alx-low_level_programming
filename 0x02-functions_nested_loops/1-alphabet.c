#include "main.h"

/**
 * main - prints alphabet in lower case
 *
 * Return: Always 0
 */
int main(void)
{
	int lower, i;
	i = a;

	for(i = 'a', i <= 'z'; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
	return (0);
}
